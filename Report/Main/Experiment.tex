\section{Experiment}\label{P3}
\subsection{Theory}

It is necessary to establish identical environments for both algorithms in order to fulfill the need of obtaining test data for the analysis. 
 Each algorithm is put through its paces in a variety of scenarios, with 4, 6, and 8 golden boxes, in order to see how quickly it is able to collect all of the boxes and how accurately it can identify the boxes and collect them. 
 The algorithms are run fifteen times in each environment setup, which results in a total of forty-five simulations for each algorithm. 
 We did not employ random creation of the boxes since we wanted to ensure that each technique was implemented in the exact same setting and that the comparison was accurate. 
 As a consequence of this, the placements of the boxes remain the same in an environment that contains the same quantity of boxes. It has been agreed that a paired T-test would be performed on the data that was extracted. 
 When we simulated both algorithms 45 times with the same amount of randomly generated golden boxes in the environment, we could have also used a two-sample T-test. This would have allowed us to obtain the same results.

The following is the recommended implementation of this method:
\begin{itemize}
    \item The difference between the two observations for each simulation pair of two algorithms should be calculated using the formula $d_i = y_i - x_i$. It is important to differentiate between positive and negative differences while doing this calculation.
    \item The mean difference ($\bar{d}$) should be calculated.
    \item The standard error of the mean difference can be calculated by calculating the standard deviation of the differences, which is denoted by $s_d$. The formula for calculating the standard error of the mean difference is $SE(\bar{d}) = \frac{s_d}{\sqrt{n}}$.
    \item Determine the t-statistic, denoted as T, using the formula T = $\frac{\bar{d}}{SE(\bar{d})}$. When the null hypothesis is assumed to be true, this statistic conforms to a t distribution with $n - 1$ degrees of freedom.
    \item Utilize t-distribution tables to compare your calculated value for T with the $t_{n-1}$ distribution. This will calculate the p-value for the paired t-test.
\end{itemize}

\subsection{Results}
The results of my code and the code simulation time and success of my colleague are provided 
in the following table under a variety of various conditions. 
Upon closer inspection, it is evident that both codes were totally successful in locating 
and collecting the boxes in each and every one of the cases that were studied. 
In some instances where there were only a few gold boxes, my code initially misplaced 
one of the boxes; but, later on in the simulation, the same box was picked up once more 
and placed near to other boxes. Due to the fact that the robot has ultimately gathered 
all of the boxes adjacent to each other, which is the sole requirement for determining 
whether or not the algorithm was successful, this particular instance cannot be called 
a failure. In spite of this, the simulation duration of the algorithm is finally impacted 
by the fact that the robot has picked up and moved a box over twice. 
As a consequence of this, the impact of this very minor error is projected in the 
computation of the algorithms' performance evaluation.
\par
procedure The paired T-test value, as determined from the observations and 
calculations in the Table, is precisely 15.35. The data is subjected to 
a paired T-test with a confidence level of 99.5\% and 29 degrees of freedom. 
According to a one-tail T-test table, we need to compare our calculated t-value 
with the value of 1.699.

\newpage
\begin{figure}[htbp]
    \centering  % Optional: centers the image horizontally
    \includegraphics{./Res.png}  % Replace with your image file name
    \caption{Result of tests}  % Optional: adds a caption below the image
    \label{fig:test_results}  % Replace with a meaningful label name
\end{figure}

\clearpage
\begin{figure}[htbp]
    \centering  % Optional: centers the image horizontally
    \includegraphics{./Graph.png}  % Replace with your image file name
    \caption{Graph for comparino between two codes}  % Optional: adds a caption below the image
    \label{fig:test_results}  % Replace with a meaningful label name
\end{figure}

\par
As can be seen by the gray line that is contained within the box on the plot, 
my method has a median execution time that is equal to 56.87 seconds. 
As a result of the very small interquartile range (IQR) for this method, 
it is likely that there is less variation in execution times around the median.
When compared to this, the method that my colleague developed demonstrates 
a median execution time of 84.25 seconds, albeit having a far wider interquartile range. 
It is possible for his algorithm to occasionally attain very quick execution speeds and 
also swing to significantly slower executions, as indicated by the fact that its 
total execution times range from approximately 48 seconds to 120 seconds.
\\
On the condition that the t value that we obtained is more than 1.699, 
the null hypothesis (H0) is rejected and the alternative hypothesis (Ha) is accepted. 
As can be observed, the t value that we obtained is far higher than 1.699, 
which demonstrates that the null hypothesis is not supported and demonstrates 
that my code is significantly more efficient than other code in terms of finishing this work.





  




\newpage